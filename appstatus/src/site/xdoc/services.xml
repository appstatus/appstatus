<?xml version="1.0" encoding="UTF-8"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/XDOC/2.0 http://maven.apache.org/xsd/xdoc-2.0.xsd">

	<properties>
		<title>Services performance monitoring</title>
		<author email="nicolas.richeton@capgemini.com">Nicolas Richeton</author>
	</properties>

	<body>

		<h1>Services performance monitoring</h1>

		<p>App status allows to simply monitor external services performance, including cache efficiency. The overhead of service monitoring should be very low, but don't use this on very simple code (no external calls, single line implementation, ...), where overhead could become significant.</p>
		
		
		<h2>Usage</h2>
		
		<p>Call appstatus before and after calls</p>
		<source><![CDATA[
IServiceMonitor monitor = appstatus.getServiceMonitor( name, group) ;
monitor.beginCall();

// If cache is used :
monitor.cacheHit();

// else perform some work here

monitor.endCall();

]]></source>	

<h2>Result</h2>
<p>AppStatus will now monitor : hits, cache hits, running calls, min/max/avg times with and without cache.</p>

<p>Go to http://&lt;server&gt;/&lt;app-context&gt;/status?p=services :</p>

<img src="images/appstatus-0.0.7-services.png" alt="Services page example" style="border: 1px solid black;"/>

	</body>
</document>
  
  